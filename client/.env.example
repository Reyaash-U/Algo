# Backend API base URL
VITE_API_BASE_URL=http://localhost:5000/api

# Mock/real toggle — see docs/ARCHITECTURE.md
# true  = use shared/contract mock*() factories, no backend needed
# false = hit the real API (server/ must be running)
VITE_USE_MOCKS=true
